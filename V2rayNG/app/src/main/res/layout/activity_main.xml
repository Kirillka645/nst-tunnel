<?xml version="1.0" encoding="utf-8"?>
<!--
    Edge-to-edge ready main layout.

    The DrawerLayout is the window root: it intentionally has NO `fitsSystemWindows="true"`
    because window insets are dispatched from MainActivity.applyEdgeToEdgeInsets() to the
    children that actually need them (Toolbar, FAB, NavigationView, layout_test).

    Background is `?attr/colorSurface` so the screen blends with the splash and the system
    bars under edge-to-edge — no flash of black on launch.
-->
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!--
            AppBar with M3 lift-on-scroll: the bar gains elevation tint as content scrolls
            under it, replacing the old hardcoded shadow.
        -->
        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorSurface"
            app:liftOnScroll="true"
            app:liftOnScrollColor="?attr/colorSurfaceContainer">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorSurface"
                app:titleTextAppearance="?attr/textAppearanceTitleLarge"
                app:titleTextColor="?attr/colorOnSurface" />

        </com.google.android.material.appbar.AppBarLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.coordinatorlayout.widget.CoordinatorLayout
                android:id="@+id/main_content"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical">

                    <!-- Indeterminate progress lives directly under the toolbar; recolours via theme. -->
                    <com.google.android.material.progressindicator.LinearProgressIndicator
                        android:id="@+id/progress_bar"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:indeterminate="true"
                        android:visibility="invisible"
                        app:indicatorColor="?attr/colorSecondary"
                        app:trackColor="?attr/colorSurfaceContainerHigh" />

                    <com.google.android.material.tabs.TabLayout
                        android:id="@+id/tab_group"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="?attr/colorSurface"
                        app:tabIndicatorAnimationMode="elastic"
                        app:tabIndicatorColor="?attr/colorSecondary"
                        app:tabIndicatorFullWidth="false"
                        app:tabMode="scrollable"
                        app:tabRippleColor="?attr/colorSurfaceContainerHigh"
                        app:tabSelectedTextColor="?attr/colorSecondary"
                        app:tabTextAppearance="@style/TabLayoutTextStyle"
                        app:tabTextColor="?attr/colorOnSurfaceVariant" />

                    <androidx.viewpager2.widget.ViewPager2
                        android:id="@+id/view_pager"
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        android:layout_weight="1"
                        android:background="?attr/colorSurface"
                        android:nextFocusRight="@+id/fab"
                        android:scrollbars="vertical" />

                    <!--
                        Bottom test-status strip (latency / "Connected" / "Not connected").
                        The activity adds a bottom system-bar inset as padding, keeping the
                        thin top divider exactly at the top regardless of gesture nav size.
                    -->
                    <LinearLayout
                        android:id="@+id/layout_test"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="?attr/colorSurfaceContainerLow"
                        android:clickable="true"
                        android:contentDescription="@string/connection_test_pending"
                        android:focusable="true"
                        android:minHeight="@dimen/view_height_dp64"
                        android:nextFocusLeft="@+id/view_pager"
                        android:nextFocusRight="@+id/fab"
                        android:orientation="vertical">

                        <View
                            android:layout_width="match_parent"
                            android:layout_height="1dp"
                            android:background="?attr/colorOutlineVariant" />

                        <TextView
                            android:id="@+id/tv_test_state"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:gravity="start|center_vertical"
                            android:maxLines="2"
                            android:minHeight="@dimen/view_height_dp64"
                            android:minLines="1"
                            android:paddingStart="@dimen/padding_spacing_dp16"
                            android:paddingEnd="@dimen/padding_spacing_dp16"
                            android:text="@string/connection_test_pending"
                            android:textAppearance="?attr/textAppearanceBodyMedium"
                            android:textColor="?attr/colorOnSurfaceVariant" />

                    </LinearLayout>
                </LinearLayout>

                <!--
                    FAB now lives directly inside the CoordinatorLayout (no wrapping FrameLayout
                    with a negative margin hack). Bottom inset + offset is applied programmatically
                    in MainActivity.applyEdgeToEdgeInsets() so the button always clears the bottom
                    test strip and the gesture nav, even when the IME is open.
                -->
                <com.google.android.material.floatingactionbutton.FloatingActionButton
                    android:id="@+id/fab"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="bottom|end"
                    android:layout_marginEnd="@dimen/padding_spacing_dp16"
                    android:layout_marginBottom="@dimen/view_height_dp36"
                    android:clickable="true"
                    android:contentDescription="@string/tasker_start_service"
                    android:focusable="true"
                    android:nextFocusLeft="@+id/layout_test"
                    android:src="@drawable/ic_play_24dp"
                    app:backgroundTint="@color/color_fab_inactive"
                    app:tint="?attr/colorOnSecondary"
                    app:useCompatPadding="true"
                    tools:ignore="ContentDescription" />

            </androidx.coordinatorlayout.widget.CoordinatorLayout>
        </RelativeLayout>
    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="?attr/colorSurfaceContainer"
        android:fitsSystemWindows="false"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/menu_drawer" />

</androidx.drawerlayout.widget.DrawerLayout>
