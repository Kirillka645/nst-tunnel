name: nst_tunnel
description: NST Tunnel desktop client — a clean, modern UI shell.
publish_to: 'none'
version: 2.17.4+1726

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
